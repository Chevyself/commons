package me.googas.annotations;

/**
 * This annotation implies that an object can be null. This is used because lombok does not include
 * a nullable annotation this maybe used in IDEs that allow to select a nullable annotation
 */
public @interface Nullable {}
